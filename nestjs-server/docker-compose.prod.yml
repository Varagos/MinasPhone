version: '3'
name: minas_phone_prod

services:
  backend:
    image: registry.heroku.com/minas-phone-api/backend
    build:
      context: .
      dockerfile: Dockerfile
    restart: always
    ports:
      - 80:8080
    environment:
      - NODE_ENV=production
